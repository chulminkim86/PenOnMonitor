# PenOnMonitor — 바로 쓰는 필기 도구

문서·영상·웹페이지 등 지금 보고 있는 화면 위에 바로 필기하거나, 
화면 전체를 흰 판으로 덮고 판서할 수 있습니다. Windows 전용.

강의 때 실사용 목적으로 제작했습니다.

## 세 가지 상태

| 상태 | 화면 | 마우스 |
|---|---|---|
| **통과 모드** (기본) | 필기가 화면에서 감춰짐 | 아래 프로그램으로 그대로 통과 |
| **그리기 모드** | 살아 있는 화면 위에 그대로 필기 (정지 없음) | 오버레이가 받아서 필기 |
| **화이트보드** | 화면 전체를 흰 판으로 덮음, 확대/축소 가능 | 오버레이가 받아서 판서 |

- **그리기 모드** — 툴바 오른쪽 스위치 또는 `F8`
- **화이트보드** — 툴바의 `화이트보드` 버튼 또는 `F7`. 그리기가 켜져 있든
  꺼져 있든 곧바로 전환됩니다.
- **Esc** — 어느 상태에서든 전부 해제한다. 화이트보드에서 나오고, 그리기를 끄고,
  **화면에 한 필기를 지운 채** 대기 상태로 돌아간다. 화이트보드 판서는 남는다.
  실수로 지웠으면 그리기를 켜고 `Ctrl+Z`로 되살릴 수 있다.

툴바는 어떤 상태에서도 항상 보이며, 상태를 전환해도 사라지거나 깜빡이지 않습니다. 

## 창 크기와 자리

**툴바는 모니터 밖으로 나가지 않는다.** 툴바는 로고를 잡고 끌 때, 메모창은 제목 표시줄을 잡고 끌 때 각각 걸린다. 
가두는 기준은 **마우스 커서가 있는 모니터**다. 

실행 중에 해상도나 모니터 구성이 바뀌면 크기를 다시 재고, 화면 밖에 남은
창을 도로 끌어온다. 메모창이 새 화면보다 커졌으면 줄여서 들여놓는다.

| 화면 | 툴바 | 화면 폭 대비 |
|---|---|---|
| 3840x2160 @200% | 1140 x 52 | 29.7% |
| 1920x1080 @150% | 760 x 34 | 39.6% |
| 1920x1080 @100% | 587 x 26 | 30.6% |
| 1366x768 @100% | 538 x 24 | 39.4% |
| 1280x720 @100% | 506 x 23 | 39.5% |

## 단축키

전역 단축키 — 초점이 어디에 있든 동작한다.

| 키 | 동작 |
|---|---|
| `F8` | 그리기 모드 켜기/끄기 |
| `F7` | 화이트보드 켜기/끄기 |
| `Esc` | 전부 해제 (화이트보드 판서는 유지) |
| `F9` | 전체 지우기 |
| `Ctrl+Alt+Q` | 종료 |

`F7`·`F8`·`F9`는 떠 있는 동안 계속 잡고 있으므로, 그 키를 쓰는 다른 프로그램에서는 동작하지 않는다. 

`Esc`는 **되돌릴 상태가 있을 때만** 가로챈다. 그리기·화이트보드가 켜져 있거나
화면에 필기가 남아 있는 동안에만 잡고, 대기 상태가 되면 즉시 놓아주므로 평소
다른 프로그램의 `Esc`를 방해하지 않는다.

| 키 | 동작 |
|---|---|
| `Ctrl+1` ~ `Ctrl+4` | 펜 / 지우개 / 사각 / 형광 |
| `Alt+1` ~ `Alt+3` | 검정 / 빨강 / 파랑 |
| `Ctrl+휠`, `Ctrl+PageUp`/`PageDown` | 굵기 1~50pt |
| `휠`, `PageUp`/`PageDown` | 화이트보드 확대 / 축소 |
| `H` | 화이트보드 손 도구 — 끌어서 이동 |
| `Ctrl+Z` / `Ctrl+Y` | 되돌리기 / 다시 실행 (툴바에는 두지 않았다) |
| 가운데 버튼 드래그 | 화이트보드 이동 (손 도구를 켜지 않아도 된다) |
| `Ctrl` + 왼쪽 드래그 | 손 도구 — 화면을 끌어 옮긴다 |
| `Ctrl`+`Shift` + 드래그 | 세로로 끌면 확대/축소(아래가 확대), 가로로 끌면 굵기(오른쪽이 굵게) |
| 오른쪽 클릭 | 전부 해제 (`Esc`와 같다) |

툴바의 각 버튼에 마우스를 올리면 해당 단축키가 툴팁으로 뜬다.

## 도구와 색상

- **펜** — 자유 곡선
- **지우개** — 커서가 닿은 획을 통째로 지운다
- **사각** — 드래그하는 동안 미리보기가 따라온다
- **형광** — 반투명(디더링) 굵은 선. **색상 선택과 무관하게 항상 노란색**
  (`#FFE24D`)이다.

| 키 | 이름 | 값 |
|---|---|---|
| `Alt+1` | 검정 (기본) | `#0D0D0D` |
| `Alt+2` | 빨강 | `#A20000` |
| `Alt+3` | 파랑 | `#2569A7` |

### 굵기

굵기 메뉴는 없다. **`Ctrl+휠`로 1pt부터 50pt까지 연속으로** 고르며, 그리기 모드와
화이트보드에서 똑같이 동작한다. 굵기를 바꾸는 동안 커서 옆에 실제 크기의 원과
`12 pt` 같은 숫자가 잠깐 떠서 눈으로 확인할 수 있다.

**굵기는 도구마다 따로 기억한다.** 기본값은 펜 5pt, 사각 5pt, 형광 24pt,
지우개 20pt이다. 형광을 굵게 써 두고 펜으로 돌아와도 펜 굵기는 그대로다.

## 화이트보드

- **확대/축소** — 그냥 휠을 돌린다. 커서 아래 지점을 고정한 채 확대되고, 배율은
  25%~800% 범위다.
- **이동** — `H`를 눌러 손 도구를 켜면 왼쪽 버튼으로 끌어 옮길 수 있다. 손 도구가
  켜지면 커서가 손 모양으로 바뀐다. 다시 `H`를
  누르거나 도구를 고르면 풀린다. 손 도구를 켜지 않아도 가운데 버튼 드래그로 옮길
  수 있다.
- 획의 굵기도 배율을 따라간다.
- **`Ctrl`+`Shift` + 드래그** — 세로로 끌면 배율, 가로로 끌면 굵기다.

## 메모

툴바의 `메모` 버튼으로 여닫는다. 윈도우 메모장 수준의 단순한 글쓰기 창이고,
가장자리를 끌어 크기를 자유롭게 바꾼다.

- **자리와 크기** — 툴바 바로 아래, 가로세로 모두 툴바 너비의 절반인 정사각형.
  창 가운데가 툴바의 `메모` 버튼 가운데에 오도록 놓인다.
- **글자 크기** — `Ctrl+휠` 로 8pt에서 96pt까지.
  - **글을 드래그해 두고 돌리면 고른 곳만 바뀐다.** 나머지는 그대로다.
  - 고른 곳이 없으면 문서 전체가 바뀐다. 이때도 한 크기로 뭉개지 않고 각
    부분을 제 크기에서 한 단계씩 옮긴다. 제목만 크게 해 둔 메모를 전체
    축소했는데 제목까지 본문 크기로 내려앉으면 서식을 잃기 때문이다.
  - 그림은 글자 크기를 따라가지 않는다. 크기는 가장자리를 끌어서 바꾼다.
- **항상 띄워놓기** — 창 위쪽 체크상자(기본 켜짐). 켜면 다른 어떤 창도 메모를
  가리지 못하고 **바탕화면 보기(Win+D)를 눌러도 사라지지 않는다.** 도구 창이라
  제목 표시줄이 작고 최소화 단추가 없다. 끄면 평범한 창이 된다.
- **그림 붙여넣기** — `Ctrl+V` 로 그림을 붙인다. 화면 캡처(Win+Shift+S)를
  그대로 붙일 수 있고, 그림 파일을 끌어다 놓아도 된다. 글을 붙일 때는
  서식을 버리고 글자만 들어온다(메모장처럼 쓰려는 것이다).
  폭이 1400px 을 넘는 그림은 줄여서 담는다.
- **그림 크기 조절** — 그림의 오른쪽·아래 가장자리나 모서리를 잡아 끌면
  비율을 지키며 크기가 바뀐다. 가장자리에 가면 커서가 바뀌어 알 수 있다.
  바꾼 크기는 저장된다.
- **내용 보존** — 창을 닫아도 남고, 입력이 멈추면 `memo.html` 한 장에
  자동 저장된다. 
  옮겨도 흩어지지 않는다. 브라우저로 바로 열어 볼 수도 있다.

